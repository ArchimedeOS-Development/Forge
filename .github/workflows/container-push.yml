name: Build and Push Container

on:
  push:
    branches: [ main ]  # ou la branche de ton choix
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU (pour multi-arch, optionnel)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx (optionnel)
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: ./Forge/base-container
          push: true
          tags: ghcr.io/${{ github.repository_owner.toLowerCase() }}/archimedeos-base:latest